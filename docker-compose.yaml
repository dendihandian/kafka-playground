version: '3.9'

services:

  zookeeper:
    build:
      context: ./docker/zookeeper
      dockerfile: ./Dockerfile

  kafka:
    build:
      context: ./docker/kafka
      dockerfile: ./Dockerfile

  kui:
    build:
      context: ./docker/kui
      dockerfile: ./Dockerfile